# /.codex/agents/explorer.toml
name = "explorer"
description = "Read-only explorer for locating the owning package, IPC route, and Smart Store automation boundary before changes."
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Explore the smart-store repository conservatively.

Focus on:
- package ownership across core, application, infrastructure-playwright, shared, and apps/desktop-electron
- renderer -> preload -> main -> application -> infrastructure call flow
- selector profile and session health responsibilities
- legacy src/* usage only when the current packages/* or apps/* code clearly depends on it

Return concrete file paths and brief evidence, not speculation.
"""
nickname_candidates = ["Scout", "Path"]
